{% extends "::layout.html.twig" %}
{% block title %}

  News - {{ parent() }}

{% endblock %}

{% block body %}

  {# On définit un sous-titre commun à toutes les pages du bundle, par exemple #}
{% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}

    Connecté en tant que <a href="{{ path('fos_user_profile_show') }}"><FONT COLOR="#FFFFFF">{{ app.user.username }}</a> {% endif %}
  <h1>News</h1>  {% if is_granted('ROLE_ADMIN') %}<center><a href="{{ path('sn_sys_news_add') }}"style="text-decoration:none"><FONT COLOR="#FFFFFF">Ajouter un News</a></center>{% endif %}

  <hr>

  {# On définit un nouveau bloc, que les vues du bundle pourront remplir #}

  {% block snsysnews_body %}

  {% endblock %}

{% endblock %}